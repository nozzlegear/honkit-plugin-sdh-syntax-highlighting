{
  "name": "@nozzlegear/honkit-plugin-sdh-syntax-highlighting",
  "version": "1.0.0",
  "main": "dist/index.js",
  "description": "Adds syntax highlighting to language code blocks used by The Shopify Development Handbook <https://nozzlegear.com/shopify-development-handbook>.",
  "license": "MIT",
  "scripts": {
    "validate": "tsc -p . --skipLibCheck --noEmit",
    "build": "npm run validate && tsc -p .",
    "prepack": "npm run build"
  },
  "engines": {
    "honkit": ">1.x.x"
  },
  "honkit": {
    "properties": {}
  },
  "devDependencies": {
    "honkit": "^4.0.4",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "highlightjs-cshtml-razor": "^2.1.1"
  }
}
